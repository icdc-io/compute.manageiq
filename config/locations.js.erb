window.icdcLocations = icdcLocations = {
  <%= ENV['LOC_NUMBER'].to_i %>: {
    name: '<%= ENV['LOCATION_NAME'] %>',
    description: '<%= ENV['LOC_DESCRIPTION'] %>',
    code: '<%= ENV['LOC_NUMBER'] %>',
    cpv_cloud: '<%= ENV['CPV_CLOUD'] %>'
  }
}

